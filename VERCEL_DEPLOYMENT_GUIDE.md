# 🚀 Полное руководство по деплою на Vercel и настройке домена на reg.ru

## 📋 Оглавление
1. [Подготовка проекта](#1-подготовка-проекта)
2. [Настройка Vercel конфигурации](#2-настройка-vercel-конфигурации)
3. [Деплой через Vercel CLI](#3-деплой-через-vercel-cli)
4. [Деплой через GitHub/GitLab](#4-деплой-через-githubgitlab)
5. [Настройка домена на reg.ru](#5-настройка-домена-на-regru)
6. [Проверка и тестирование](#6-проверка-и-тестирование)
7. [Решение проблем](#7-решение-проблем)

---

## 1. Подготовка проекта

### 1.1. Проверка структуры проекта
Убедитесь, что вы находитесь в директории `frontend`:
```bash
cd frontend
```

### 1.2. Проверка зависимостей
Убедитесь, что все зависимости установлены:
```bash
npm install
```

### 1.3. Проверка сборки локально
Проверьте, что проект собирается без ошибок:
```bash
npm run build
```

Если сборка успешна, вы увидите папку `dist` с собранными файлами.

### 1.4. Проверка preview сборки
Запустите preview для проверки:
```bash
npm run preview
```

Откройте `http://localhost:4173` в браузере и проверьте, что все работает.

---

## 2. Настройка Vercel конфигурации

### 2.1. Создание файла `vercel.json`

Создайте файл `vercel.json` в корне папки `frontend` со следующим содержимым:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    },
    {
      "source": "/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

**Важно:** Файл `rewrites` необходим для корректной работы React Router (SPA роутинг).

### 2.2. Обновление `vite.config.ts` (если нужно)

Убедитесь, что в `vite.config.ts` правильно настроен `base` для продакшена:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/', // Убедитесь, что base установлен в '/'
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false, // Отключите sourcemap для продакшена (опционально)
  },
})
```

---

## 3. Деплой через Vercel CLI

### 3.1. Установка Vercel CLI

Установите Vercel CLI глобально:
```bash
npm install -g vercel
```

Или используйте npx (без глобальной установки):
```bash
npx vercel
```

### 3.2. Авторизация в Vercel

Войдите в свой аккаунт Vercel:
```bash
vercel login
```

Следуйте инструкциям в браузере для авторизации.

### 3.3. Деплой проекта

Перейдите в папку `frontend`:
```bash
cd frontend
```

Запустите деплой:
```bash
vercel
```

**При первом деплое:**
1. Vercel спросит: "Set up and deploy?" → Нажмите `Y`
2. "Which scope?" → Выберите ваш аккаунт или команду
3. "Link to existing project?" → Если проект уже существует, выберите `Y` и укажите имя проекта
4. "What's your project's name?" → Введите `ringo-stroy` (или другое имя)
5. "In which directory is your code located?" → Введите `./` (текущая директория)
6. "Want to override the settings?" → Нажмите `N` (используем настройки из `vercel.json`)

**При последующих деплоях:**
Просто выполните:
```bash
vercel --prod
```

### 3.4. Проверка деплоя

После деплоя Vercel предоставит вам URL вида:
```
https://ringo-stroy-xxxxx.vercel.app
```

Откройте этот URL в браузере и проверьте работу сайта.

---

## 4. Деплой через GitHub/GitLab

### 4.1. Подготовка репозитория

Убедитесь, что ваш проект находится в Git репозитории:
```bash
cd frontend
git init  # если еще не инициализирован
git add .
git commit -m "Prepare for Vercel deployment"
```

### 4.2. Создание `.gitignore` (если нет)

Создайте файл `.gitignore` в папке `frontend`:
```
node_modules
dist
.env
.env.local
.DS_Store
*.log
.vercel
```

### 4.3. Push в репозиторий

Загрузите код в GitHub/GitLab:
```bash
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git push -u origin main
```

### 4.4. Подключение к Vercel через веб-интерфейс

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Нажмите **"Add New..."** → **"Project"**
4. Выберите ваш репозиторий
5. Настройте проект:
   - **Framework Preset:** Vite
   - **Root Directory:** `frontend` (если репозиторий содержит несколько проектов)
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
6. Нажмите **"Deploy"**

### 4.5. Автоматический деплой

После первого деплоя Vercel автоматически будет деплоить проект при каждом push в основную ветку (обычно `main` или `master`).

---

## 5. Настройка домена на reg.ru

### 5.1. Получение DNS записей от Vercel

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings** → **Domains**
3. Нажмите **"Add Domain"**
4. Введите ваш домен (например, `ringoostroy.ru` или `www.ringoostroy.ru`)
5. Vercel покажет вам DNS записи, которые нужно добавить:
   - **A Record** или **CNAME Record**
   - Обычно это что-то вроде:
     ```
     Type: CNAME
     Name: @ или www
     Value: cname.vercel-dns.com
     ```
     Или:
     ```
     Type: A
     Name: @
     Value: 76.76.21.21 (IP адрес Vercel)
     ```

### 5.2. Настройка DNS в reg.ru

1. Войдите в личный кабинет [reg.ru](https://www.reg.ru)
2. Перейдите в раздел **"Домены"**
3. Найдите ваш домен и нажмите **"Управление"**
4. Перейдите в раздел **"DNS-серверы и зона"**
5. Выберите **"Использовать DNS-серверы REG.RU"** (если еще не выбрано)
6. Перейдите в раздел **"Управление DNS-записями"**

### 5.3. Добавление DNS записей

#### Вариант 1: Использование CNAME (рекомендуется)

1. Нажмите **"Добавить запись"**
2. Выберите тип **CNAME**
3. Заполните:
   - **Имя:** `@` (для основного домена) или `www` (для поддомена www)
   - **Каноническое имя:** `cname.vercel-dns.com` (или значение, которое дал Vercel)
   - **TTL:** `3600` (или оставьте по умолчанию)
4. Нажмите **"Добавить"**

**Важно:** Если вы используете `@` для CNAME, убедитесь, что ваш регистратор поддерживает это. Некоторые регистраторы требуют использовать A-запись для корневого домена.

#### Вариант 2: Использование A-записи (для корневого домена)

Если reg.ru не поддерживает CNAME для корневого домена (`@`):

1. Нажмите **"Добавить запись"**
2. Выберите тип **A**
3. Заполните:
   - **Имя:** `@`
   - **IP-адрес:** `76.76.21.21` (или IP, который дал Vercel)
   - **TTL:** `3600`
4. Нажмите **"Добавить"**

#### Вариант 3: Комбинированный подход

1. Для корневого домена (`@`): используйте **A-запись** с IP от Vercel
2. Для поддомена `www`: используйте **CNAME** с `cname.vercel-dns.com`

### 5.4. Пример конфигурации DNS

```
Тип    Имя    Значение                    TTL
A      @      76.76.21.21                 3600
CNAME  www    cname.vercel-dns.com        3600
```

### 5.5. Ожидание распространения DNS

После добавления DNS записей:
- **Обычное время:** 5-30 минут
- **Максимальное время:** до 48 часов (редко)

Проверить распространение DNS можно через:
- [whatsmydns.net](https://www.whatsmydns.net)
- Команда в терминале: `nslookup ваш-домен.ru`

### 5.6. Подтверждение домена в Vercel

1. Вернитесь в Vercel Dashboard → **Settings** → **Domains**
2. Дождитесь, пока статус домена изменится на **"Valid Configuration"**
3. Если статус показывает ошибку, проверьте DNS записи еще раз

### 5.7. Настройка SSL сертификата

Vercel автоматически выдаст SSL сертификат (HTTPS) для вашего домена после успешной настройки DNS. Это может занять несколько минут.

---

## 6. Проверка и тестирование

### 6.1. Проверка основного функционала

После деплоя проверьте:

1. ✅ Главная страница открывается
2. ✅ Все маршруты работают (SPA роутинг)
3. ✅ Изображения загружаются
4. ✅ Стили применяются корректно
5. ✅ Мобильная версия работает
6. ✅ SEO мета-теги присутствуют

### 6.2. Проверка производительности

Используйте инструменты:
- [PageSpeed Insights](https://pagespeed.web.dev/)
- [GTmetrix](https://gtmetrix.com/)
- Vercel Analytics (если включен)

### 6.3. Проверка HTTPS

Убедитесь, что сайт доступен по HTTPS:
```
https://ringoostroy.ru
```

### 6.4. Проверка редиректов

Проверьте, что:
- `http://ringoostroy.ru` → редирект на `https://ringoostroy.ru`
- `http://www.ringoostroy.ru` → редирект на `https://ringoostroy.ru`
- `https://www.ringoostroy.ru` → редирект на `https://ringoostroy.ru` (или наоборот, в зависимости от настроек)

---

## 7. Решение проблем

### 7.1. Проблема: 404 ошибка на всех страницах кроме главной

**Причина:** Не настроены rewrites для SPA роутинга.

**Решение:** Убедитесь, что в `vercel.json` есть секция `rewrites`:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 7.2. Проблема: Домен не подключается

**Причины и решения:**

1. **DNS записи еще не распространились**
   - Подождите до 48 часов
   - Проверьте через [whatsmydns.net](https://www.whatsmydns.net)

2. **Неправильные DNS записи**
   - Проверьте точность ввода в reg.ru
   - Убедитесь, что используете правильные значения от Vercel

3. **Конфликтующие DNS записи**
   - Удалите старые A/CNAME записи для домена
   - Оставьте только записи для Vercel

### 7.3. Проблема: Изображения не загружаются

**Причина:** Неправильные пути к изображениям.

**Решение:** Убедитесь, что в коде используются относительные пути:
```typescript
// ✅ Правильно
<img src="/images/icons/logo.svg" />

// ❌ Неправильно
<img src="./images/icons/logo.svg" />
```

### 7.4. Проблема: Стили не применяются

**Причина:** Проблемы с Tailwind CSS или PostCSS.

**Решение:** 
1. Проверьте, что `tailwind.config.js` и `postcss.config.js` настроены правильно
2. Убедитесь, что все CSS файлы импортируются в `main.tsx`

### 7.5. Проблема: Ошибки сборки на Vercel

**Решение:**
1. Проверьте логи сборки в Vercel Dashboard
2. Убедитесь, что версия Node.js совместима (Vercel использует Node.js 18.x по умолчанию)
3. Добавьте файл `.nvmrc` в корень `frontend`:
   ```
   18
   ```

### 7.6. Проблема: Медленная загрузка

**Решения:**
1. Включите сжатие в `vercel.json` (автоматически включено)
2. Оптимизируйте изображения (используйте WebP формат)
3. Включите кэширование статических ресурсов
4. Используйте Vercel Analytics для мониторинга

---

## 8. Дополнительные настройки

### 8.1. Переменные окружения

Если нужны переменные окружения:

1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Добавьте переменные:
   - `VITE_API_URL` (если используется)
   - Другие переменные с префиксом `VITE_`

**Важно:** В Vite переменные окружения должны начинаться с `VITE_` для доступа в клиентском коде.

### 8.2. Настройка редиректов

Добавьте в `vercel.json`:
```json
{
  "redirects": [
    {
      "source": "/old-page",
      "destination": "/new-page",
      "permanent": true
    }
  ]
}
```

### 8.3. Настройка заголовков безопасности

Расширенные заголовки уже добавлены в `vercel.json`. При необходимости добавьте:
```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=31536000; includeSubDomains"
        },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'"
        }
      ]
    }
  ]
}
```

### 8.4. Включение Vercel Analytics

1. В Vercel Dashboard → **Analytics**
2. Включите **Web Analytics**
3. Добавьте скрипт в `index.html` (если требуется)

---

## 9. Чек-лист перед деплоем

- [ ] Проект собирается локально (`npm run build`)
- [ ] Все зависимости установлены
- [ ] Создан файл `vercel.json`
- [ ] Настроены rewrites для SPA
- [ ] Проверены пути к изображениям и ресурсам
- [ ] Проверена работа на всех страницах
- [ ] Проверена мобильная версия
- [ ] Обновлены SEO мета-теги с правильным доменом
- [ ] Проверены все ссылки в коде

---

## 10. Контакты и поддержка

- **Vercel Documentation:** [vercel.com/docs](https://vercel.com/docs)
- **Vercel Support:** [vercel.com/support](https://vercel.com/support)
- **reg.ru Support:** [www.reg.ru/support](https://www.reg.ru/support)

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
- **Vercel URL:** `https://ringo-stroy-xxxxx.vercel.app`
- **Ваш домен:** `https://ringoostroy.ru`

Удачи с деплоем! 🚀

