# 🔧 Исправление проблемы с деплоем на Vercel

## Проблема

В логах видно предупреждение:
```
WARN! Due to `builds` existing in your configuration file, the Build and Development Settings defined in your Project Settings will not apply.
```

Это означает, что в `vercel.json` используется старый формат конфигурации, который конфликтует с автоматическими настройками Vercel для Vite проектов.

## Решение

### Шаг 1: Исправление vercel.json

Я уже обновил файл `frontend/vercel.json` - убрал старые поля `version: 2` и `routes`, добавил правильные `rewrites` для SPA.

**Текущий файл `vercel.json`:**
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/images/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

### Шаг 2: Проверка настроек проекта в Vercel Dashboard

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Выберите проект `ringo-stroy`
3. Перейдите в **Settings** → **General**
4. Проверьте настройки:
   - **Root Directory:** должно быть `frontend` (если проект в подпапке)
   - **Framework Preset:** должно быть `Vite` (определится автоматически)
   - **Build Command:** должно быть `npm run build` (по умолчанию)
   - **Output Directory:** должно быть `dist` (по умолчанию)
   - **Install Command:** должно быть `npm install` (по умолчанию)

### Шаг 3: Удаление устаревших полей из vercel.json

Если в репозитории есть старый `vercel.json` с полями `builds` или `version: 2`, их нужно удалить, так как они конфликтуют с автоматическими настройками Vercel.

### Шаг 4: Коммит изменений

1. Добавьте изменения в Git:
   ```bash
   cd frontend
   git add vercel.json
   git commit -m "Исправление vercel.json для правильной работы с Vite SPA"
   git push
   ```

2. Vercel автоматически пересоберет проект после push

### Шаг 5: Проверка деплоя

1. Откройте Vercel Dashboard → Deployments
2. Дождитесь завершения нового деплоя
3. Проверьте, что предупреждение исчезло
4. Проверьте работу сайта на временном URL Vercel

## Важные моменты

1. **Root Directory:** Если проект находится в подпапке `frontend/`, убедитесь, что в Vercel Dashboard → Settings → General указан Root Directory как `frontend`

2. **Автоматические настройки:** Vercel автоматически определяет Vite проекты и настраивает их правильно. Не нужно указывать `buildCommand`, `outputDirectory` и другие настройки в `vercel.json`, если они стандартные.

3. **Rewrites для SPA:** Для Vite SPA приложения нужно использовать `rewrites`, а не `routes`. `rewrites` перенаправляет все запросы на `index.html`, что необходимо для работы React Router.

4. **Кэширование:** Заголовки `Cache-Control` для статических файлов (изображения, CSS, JS) помогут улучшить производительность.

## Проверка работы

После исправления:

1. ✅ Предупреждение о `builds` должно исчезнуть
2. ✅ Сайт должен открываться на временном URL Vercel
3. ✅ Все страницы должны работать (включая `/services`, `/about`, и т.д.)
4. ✅ Изображения должны загружаться
5. ✅ Формы должны работать

## Если проблема сохраняется

1. **Проверьте логи сборки:**
   - Откройте Vercel Dashboard → Deployments → выберите последний деплой → Logs
   - Проверьте, есть ли ошибки

2. **Проверьте настройки проекта:**
   - Убедитесь, что Root Directory указан правильно
   - Убедитесь, что Framework Preset установлен как Vite

3. **Проверьте структуру проекта:**
   - Убедитесь, что `package.json` находится в правильной папке
   - Убедитесь, что `vite.config.ts` существует

4. **Проверьте локальную сборку:**
   ```bash
   cd frontend
   npm run build
   ```
   - Если локальная сборка не работает, исправьте ошибки

5. **Обратитесь в поддержку Vercel:**
   - https://vercel.com/support
   - Приложите логи сборки и описание проблемы

---

**Дата исправления:** 2025-01-27  
**Версия:** 1.1

