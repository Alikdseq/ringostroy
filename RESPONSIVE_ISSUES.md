# 📱 Анализ проблем адаптивности сайта

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (не работают на мобильных)

### 1. **Header.tsx** (строки 15-182)
**Проблемы:**
- ❌ Строка 30: `paddingLeft: '50px'` - слишком большой отступ для мобильных
- ❌ Строки 45-63: Rating Block всегда виден, не скрыт на мобильных
- ❌ Строки 66-106: Навигация использует `className="hidden md:flex"` (Tailwind), но кнопка бургера не работает (нет мобильного меню)
- ❌ Строки 109-168: Кнопка контакта скрыта на мобильных, но нет альтернативы
- ❌ Строки 170-178: Бургер-кнопка есть, но не открывает меню (isMenuOpen не используется)

**Что нужно:**
- Добавить полноценное мобильное меню с overlay
- Уменьшить paddingLeft на мобильных
- Скрыть Rating Block на мобильных или сделать компактным
- Добавить мобильную версию кнопки контакта в меню

---

### 2. **ServicesCarousel.tsx** (строки 145-372)
**Проблемы:**
- ❌ Строка 194: `flex: '0 0 calc(50% - 10px)'` - ВСЕГДА показывает 2 карточки, не адаптируется на мобильных
- ❌ Строка 210: `height: '300px'` - фиксированная высота изображений
- ❌ Строки 149-173: Стрелки навигации фиксированного размера (48px), могут перекрывать контент на мобильных
- ❌ Строки 182-186: gap: '20px' - может быть слишком большой для мобильных

**Что нужно:**
- На мобильных: `flex: '0 0 100%'` (1 карточка)
- На планшетах: `flex: '0 0 calc(50% - 10px)'` (2 карточки)
- Адаптировать размер стрелок на мобильных
- Уменьшить gap на маленьких экранах

---

### 3. **Contacts.tsx** (строки 80-253)
**Проблемы:**
- ❌ Строка 82: `gridTemplateColumns: '1fr 1.5fr'` - ВСЕГДА две колонки, карта и блоки рядом
- ❌ Строка 206: `height: '600px'` - фиксированная высота карты
- ❌ Строки 96-97, 129-130, 153-154: `padding: '30px'` - может быть слишком много для мобильных
- ❌ Строка 166: `gridTemplateColumns: 'repeat(2, 1fr)'` - города всегда в 2 колонки

**Что нужно:**
- На мобильных: `gridTemplateColumns: '1fr'` (стек сверху вниз)
- Адаптировать высоту карты: `height: isMobile ? '400px' : '600px'`
- Уменьшить padding на мобильных: `padding: isMobile ? '20px' : '30px'`
- Города в 1 колонку на мобильных

---

### 4. **Promotions.tsx** (строки 80-283)
**Проблемы:**
- ❌ Строка 82: `gridTemplateColumns: 'repeat(2, 1fr)'` - ВСЕГДА две колонки
- ❌ Строки 97, 196: `width: '50%'` - фиксированная ширина текстовой части и изображения
- ❌ Строки 93, 192: `flexDirection: 'row'` - всегда горизонтально, не адаптируется
- ❌ Строка 170: `minHeight: '300px'` - может быть слишком высоко для мобильных

**Что нужно:**
- На мобильных: `gridTemplateColumns: '1fr'` (1 карточка в ряд)
- На мобильных: `flexDirection: 'column'` (текст сверху, изображение снизу)
- Адаптировать ширину: `width: isMobile ? '100%' : '50%'`
- Уменьшить minHeight на мобильных

---

### 5. **About.tsx** (строки 76-402)
**Проблемы:**
- ❌ Строка 78: `gridTemplateColumns: 'repeat(2, 1fr)'` - две фотографии всегда рядом
- ❌ Строки 85, 100: `height: '400px'` - фиксированная высота изображений
- ❌ Строка 119: Использует `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4` (Tailwind) - ок, но нужно проверить
- ❌ Строки 222-402: Блок с текстом и логотипом, блок основателя - могут быть не адаптивны

**Что нужно:**
- На мобильных: `gridTemplateColumns: '1fr'` для фото
- Адаптировать высоту изображений: `height: isMobile ? '250px' : '400px'`
- Проверить все блоки на адаптивность

---

### 6. **Service Pages (Page-1.tsx до Page-19.tsx)**
**Проблемы (на примере Page-1.tsx):**
- ❌ Строка 88: `padding: '40px'` - слишком много для мобильных
- ❌ Строки 91, 177: `gap: '40px'` - слишком большой отступ
- ❌ Строки 96, 182: `flex: '1 1 500px'` и `flex: '0 1 500px'` - фиксированная минимальная ширина 500px
- ❌ Строка 148: `height: '400px'` - фиксированная высота изображений
- ❌ Строка 178: `flexWrap: 'wrap-reverse'` - может быть проблемой на мобильных
- ❌ Строка 194: `height: '500px'` - фиксированная высота второго изображения

**Что нужно:**
- Уменьшить padding на мобильных: `padding: isMobile ? '20px' : '40px'`
- Уменьшить gap: `gap: isMobile ? '20px' : '40px'`
- Изменить flex на мобильных: `flex: isMobile ? '1 1 100%' : '1 1 500px'`
- Адаптировать высоты изображений
- Изменить flexWrap на мобильных

---

### 7. **ContactQuickForm.tsx** (строка 58)
**Проблемы:**
- ❌ Строка 58: `maxWidth: 390` - фиксированная ширина, может быть слишком узкой/широкой
- ❌ Строка 58: `padding: 28` - может быть слишком много для мобильных
- ❌ Строка 59: `fontSize: 20` - может быть слишком большим для мобильных

**Что нужно:**
- Адаптивная ширина: `maxWidth: isMobile ? '100%' : '390px'`
- Адаптивный padding
- Адаптивный размер шрифта

---

### 8. **Home.tsx** (строки 63-365)
**Проблемы:**
- ❌ Строки 98, 122, 145, 168: Фиксированные размеры декоративных элементов `width: '800px', height: '800px'` - слишком большие для мобильных
- ❌ Строка 186: Hero section использует Tailwind классы, но нужно проверить адаптивность
- ❌ Строка 224: Изображение грузовика может быть не адаптивно

**Что нужно:**
- Адаптировать размеры декоративных элементов: скрыть или уменьшить на мобильных
- Проверить адаптивность Hero секции
- Убедиться что изображение грузовика адаптивно

---

### 9. **Services.tsx**
**Статус:** ✅ Уже есть `isMobile` состояние (строка 10)
**Проверить:**
- Строка 133: `gridTemplateColumns: isMobile ? '1fr' : 'repeat(2, 1fr)'` - должно работать
- Но нужно проверить карточки на адаптивность padding и размеры

---

### 10. **Footer.tsx**
**Статус:** ⚠️ Частично адаптивен (использует `flexWrap: 'wrap'`)
**Проверить:**
- Строки 18-22: `flexWrap: 'wrap'` работает, но может быть улучшено с grid
- Размеры шрифтов могут быть адаптированы
- Padding может быть уменьшен на мобильных

---

## 📋 ИТОГОВЫЙ СПИСОК ФАЙЛОВ ДЛЯ ИСПРАВЛЕНИЯ:

### Высокий приоритет:
1. ✅ **Header.tsx** - КРИТИЧНО: нет мобильного меню
2. ✅ **ServicesCarousel.tsx** - КРИТИЧНО: всегда 2 карточки
3. ✅ **Contacts.tsx** - КРИТИЧНО: всегда 2 колонки
4. ✅ **Promotions.tsx** - КРИТИЧНО: всегда 2 колонки
5. ✅ **About.tsx** - КРИТИЧНО: фото всегда 2 колонки
6. ✅ **Page-1.tsx до Page-19.tsx** - КРИТИЧНО: фиксированные размеры

### Средний приоритет:
7. ⚠️ **ContactQuickForm.tsx** - фиксированная ширина
8. ⚠️ **Home.tsx** - декоративные элементы

### Низкий приоритет:
9. ✅ **Services.tsx** - проверить детали
10. ✅ **Footer.tsx** - улучшить адаптивность

---

## 🎯 ПЛАН ИСПРАВЛЕНИЯ:

1. Добавить хук `useMobile` или использовать `useState` с `window.innerWidth` во всех компонентах
2. Заменить все фиксированные значения на адаптивные с условиями
3. Использовать `clamp()` для размеров шрифтов где возможно
4. Изменить grid/flex layouts на адаптивные (1 колонка на мобильных)
5. Уменьшить padding, gap, margin на мобильных
6. Адаптировать размеры изображений и высоты контейнеров

