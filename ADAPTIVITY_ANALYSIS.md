# Анализ адаптивности сайта

## 📋 Проблемы адаптивности по файлам

### 1. **Header.tsx** (КРИТИЧНО)
**Проблемы:**
- ❌ `paddingLeft: '50px'` у логотипа - слишком большой отступ на мобильных
- ❌ `height: '65px'` у логотипа - слишком большой для мобильных
- ❌ Rating Block всегда виден - должен скрываться на мобильных
- ❌ Navigation использует `className="hidden md:flex"` (Tailwind), но нет мобильного меню
- ❌ Кнопка бургера есть, но не работает (нет функционала открытия меню)
- ❌ Contact Button скрыт на мобильных, но нет альтернативы

**Строки:**
- 26-30: Логотип с фиксированным paddingLeft
- 36-40: Размер логотипа
- 46-63: Rating Block (не адаптивен)
- 65-106: Navigation (использует Tailwind классы, но нет мобильного меню)
- 170-178: Кнопка бургера (есть, но не работает)

---

### 2. **ServicesCarousel.tsx** (КРИТИЧНО)
**Проблемы:**
- ❌ `flex: '0 0 calc(50% - 10px)'` - всегда показывает 2 карточки, не адаптируется
- ❌ `height: '300px'` у изображений - фиксированная высота
- ❌ Стрелки навигации `width: '48px', height: '48px'` - могут быть слишком большими на мобильных
- ❌ `padding: '25px'` в карточках - может быть слишком много на мобильных
- ❌ `fontSize: '20px'` у заголовков - не адаптивный
- ❌ Кнопка "Все услуги" `paddingRight: '40px'` - может быть проблемой на мобильных

**Строки:**
- 194: `flex: '0 0 calc(50% - 10px)'` - должна быть `100%` на мобильных
- 210: `height: '300px'` - должна быть адаптивной
- 149-173: Стрелки навигации
- 250: `padding: '25px'` в карточках
- 260: `fontSize: '20px'` у заголовков

---

### 3. **Home.tsx**
**Проблемы:**
- ⚠️ Hero секция использует Tailwind классы (`lg:w-1/2`, `md:text-7xl`) - частично адаптивна
- ⚠️ Декоративные элементы с фиксированными размерами (`width: '800px'`)
- ✅ Advantages Section использует `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4` - адаптивна

**Строки:**
- 94-183: Декоративные элементы с фиксированными размерами
- 186: Hero секция (частично адаптивна через Tailwind)

---

### 4. **Services.tsx**
**Статус:** ✅ Адаптивна (использует `isMobile` и `gridTemplateColumns: isMobile ? '1fr' : 'repeat(2, 1fr)'`)

---

### 5. **About.tsx** (КРИТИЧНО)
**Проблемы:**
- ❌ `gridTemplateColumns: 'repeat(2, 1fr)'` у двух фото под заголовком - всегда 2 колонки
- ❌ `height: '400px'` у изображений - фиксированная высота
- ✅ Блок преимуществ использует `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4` - адаптивен
- ❌ Блок с текстом и логотипом - не проверен на адаптивность
- ❌ Блок основателя - не проверен на адаптивность

**Строки:**
- 76-112: Две фотографии (`gridTemplateColumns: 'repeat(2, 1fr)'`)
- 85, 100: `height: '400px'` у изображений

---

### 6. **Contacts.tsx** (КРИТИЧНО)
**Проблемы:**
- ❌ `gridTemplateColumns: '1fr 1.5fr'` - всегда 2 колонки, не адаптируется
- ❌ `height: '600px'` у карты - фиксированная высота
- ❌ `gridTemplateColumns: 'repeat(2, 1fr)'` у городов - всегда 2 колонки
- ❌ `padding: '30px'` в блоках - может быть слишком много на мобильных

**Строки:**
- 80-85: Основной контент (`gridTemplateColumns: '1fr 1.5fr'`)
- 206: `height: '600px'` у карты
- 166: `gridTemplateColumns: 'repeat(2, 1fr)'` у городов

---

### 7. **Promotions.tsx** (КРИТИЧНО)
**Проблемы:**
- ❌ `gridTemplateColumns: 'repeat(2, 1fr)'` - всегда 2 карточки в ряд
- ❌ `flexDirection: 'row'` у карточек - текст и изображение всегда в ряд
- ❌ `width: '50%'` у текстовой части и изображения - фиксированные размеры
- ❌ `minHeight: '300px'` у изображений - может быть проблемой на мобильных

**Строки:**
- 80-85: Grid контейнер (`gridTemplateColumns: 'repeat(2, 1fr)'`)
- 92-93: `flexDirection: 'row'` у карточек
- 97, 169: `width: '50%'` у текстовой части
- 170, 269: `minHeight: '300px'` у изображений

---

### 8. **Service Pages (Page-1.tsx и другие)** (КРИТИЧНО)
**Проблемы:**
- ❌ `flexWrap: 'wrap'` и `flexWrap: 'wrap-reverse'` - элементы могут неправильно располагаться
- ❌ `flex: '1 1 500px'` и `flex: '0 1 500px'` - фиксированные минимальные размеры
- ❌ `padding: '40px'` - слишком много на мобильных
- ❌ `gap: '40px'` - слишком большой отступ на мобильных
- ❌ `height: '400px'` и `height: '500px'` у изображений - фиксированные
- ❌ Галерея изображений - не проверена на адаптивность

**Строки (пример из Page-1.tsx):**
- 85-93: Фоновый блок с `padding: '40px'` и `gap: '40px'`
- 96: `flex: '1 1 500px'`
- 148: `height: '400px'`
- 178: `flexWrap: 'wrap-reverse'`

---

### 9. **ContactQuickForm.tsx**
**Проблемы:**
- ⚠️ `maxWidth: 390` - фиксированная ширина, может быть узкой на больших экранах
- ⚠️ `padding: 28` - может быть слишком много на мобильных
- ⚠️ `fontSize: 20` у заголовка - не адаптивный
- ⚠️ `fontSize: 16` у полей ввода - не адаптивный

**Строки:**
- 58: `maxWidth: 390, padding: 28`
- 59: `fontSize: 20` у заголовка

---

### 10. **CityForm.tsx**
**Проблемы:**
- ⚠️ `gridTemplateColumns: 'repeat(2, 1fr)'` у городов - всегда 2 колонки
- ⚠️ `gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))'` - может быть проблемой на маленьких экранах
- ⚠️ `padding: '30px'` - может быть слишком много на мобильных

**Строки:**
- 56: `gridTemplateColumns: 'repeat(2, 1fr)'` у городов
- 49: `gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))'`

---

### 11. **PromoBanner.tsx**
**Проблемы:**
- ⚠️ `maxWidth: 950` - фиксированная максимальная ширина
- ⚠️ `padding: '34px 28px 24px 28px'` - может быть слишком много на мобильных
- ⚠️ `minWidth: 260` у промо-блоков - может быть проблемой на маленьких экранах
- ⚠️ `gap: '32px'` - слишком большой отступ на мобильных

**Строки:**
- 36: `maxWidth: 950, padding: '34px 28px 24px 28px'`
- 40: `minWidth: 260`

---

### 12. **Footer.tsx**
**Статус:** ⚠️ Частично адаптивен (использует `flexWrap: 'wrap'`, но нет проверки на мобильные устройства)

---

## 🎯 Приоритет исправлений

### КРИТИЧНО (блокирует использование на мобильных):
1. **Header.tsx** - нет рабочего мобильного меню
2. **ServicesCarousel.tsx** - всегда 2 карточки, не адаптируется
3. **Contacts.tsx** - карта и блоки не адаптивны
4. **Promotions.tsx** - карточки не адаптивны
5. **About.tsx** - две фотографии всегда в 2 колонки
6. **Service Pages** - все блоки с фиксированными размерами

### ВАЖНО (ухудшает UX на мобильных):
7. **ContactQuickForm.tsx** - фиксированные размеры
8. **CityForm.tsx** - города всегда в 2 колонки
9. **PromoBanner.tsx** - фиксированная ширина

### УЛУЧШЕНИЯ:
10. **Home.tsx** - декоративные элементы
11. **Footer.tsx** - улучшить адаптивность

---

## 📝 Рекомендации

1. **Использовать единый подход:**
   - Создать хук `useIsMobile()` для определения размера экрана
   - Использовать медиа-запросы или состояние для адаптивности
   - Избегать фиксированных размеров в пикселях

2. **Брейкпоинты:**
   - Мобильные: < 768px
   - Планшеты: 768px - 1024px
   - Десктоп: > 1024px

3. **Паттерны для адаптивности:**
   - Grid: `gridTemplateColumns: isMobile ? '1fr' : 'repeat(2, 1fr)'`
   - Flex: `flexDirection: isMobile ? 'column' : 'row'`
   - Padding: `padding: isMobile ? '20px' : '40px'`
   - FontSize: `fontSize: clamp(16px, 4vw, 24px)`

