# 🚀 Быстрый старт: Деплой на Vercel

## 📝 Краткая инструкция

### 1. Подготовка проекта (локально)

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости (если еще не установлены)
npm install

# Соберите проект локально для проверки
npm run build

# Проверьте, что папка dist создана и содержит файлы
ls dist
```

### 2. Коммит изменений в Git

```bash
# Добавьте все изменения
git add .

# Закоммитьте изменения
git commit -m "Подготовка к деплою на Vercel"

# Отправьте изменения в репозиторий
git push
```

### 3. Деплой в Vercel

#### Вариант A: Через веб-интерфейс Vercel

1. Откройте https://vercel.com
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите **"Add New Project"** или **"Import Project"**
4. Выберите Git провайдера (GitHub, GitLab, Bitbucket)
5. Выберите репозиторий с проектом
6. **Важно:** Укажите **Root Directory** как `frontend`
7. Настройки проекта:
   - **Framework Preset:** `Vite` (определится автоматически)
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `dist` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)
8. Нажмите **"Deploy"**
9. Дождитесь завершения сборки (2-5 минут)

#### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Перейдите в папку frontend
cd frontend

# Задеплойте проект
vercel

# Следуйте инструкциям в терминале
# Выберите:
# - Set up and deploy? Yes
# - Which scope? Выберите ваш аккаунт
# - Link to existing project? No (или Yes, если проект уже существует)
# - What's your project's name? ringo-stroy
# - In which directory is your code located? ./
# - Want to override the settings? No
```

### 4. Настройка домена ringoostroy.ru

1. В Vercel Dashboard откройте проект
2. Перейдите в **Settings** → **Domains**
3. Нажмите **"Add Domain"**
4. Введите домен: `ringoostroy.ru`
5. Нажмите **"Add"**
6. Vercel покажет DNS записи, которые нужно добавить

### 5. Настройка DNS записей

1. Откройте панель управления доменом (где вы покупали домен)
2. Перейдите в раздел **DNS настройки**
3. Добавьте DNS записи, которые указал Vercel:

   **Вариант 1: A запись**
   - Имя: `@` (или оставьте пустым)
   - Тип: `A`
   - Значение: `76.76.21.21` (IP адрес Vercel)

   **Вариант 2: CNAME запись (рекомендуется)**
   - Имя: `@` (или оставьте пустым)
   - Тип: `CNAME`
   - Значение: `cname.vercel-dns.com`

   **Для www поддомена:**
   - Имя: `www`
   - Тип: `CNAME`
   - Значение: `cname.vercel-dns.com`

4. Сохраните изменения
5. Подождите распространения DNS (обычно 1-2 часа, максимум 48 часов)

### 6. Проверка деплоя

1. Проверьте статус домена в Vercel Dashboard → Settings → Domains
2. Когда появится зеленая галочка, домен готов
3. Откройте сайт в браузере: `https://ringoostroy.ru`
4. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Все страницы работают
   - ✅ Изображения загружаются
   - ✅ Формы работают
   - ✅ WhatsApp интеграция работает

### 7. Проверка SEO

1. Проверьте мета-теги:
   - Откройте исходный код страницы (Ctrl+U)
   - Проверьте наличие `<title>`, `<meta name="description">`

2. Проверьте robots.txt:
   - Откройте `https://ringoostroy.ru/robots.txt`

3. Проверьте sitemap.xml:
   - Откройте `https://ringoostroy.ru/sitemap.xml`

---

## 🔧 Решение проблем

### Проблема: Сборка падает с ошибками

**Решение:**
1. Проверьте логи сборки в Vercel Dashboard
2. Убедитесь, что TypeScript компилируется без ошибок:
   ```bash
   cd frontend
   npm run build
   ```
3. Проверьте, что все файлы закоммичены в Git

### Проблема: Изображения не загружаются

**Решение:**
1. Убедитесь, что изображения в папке `public/images/`
2. Проверьте, что пути к изображениям начинаются с `/images/`
3. Убедитесь, что изображения закоммичены в Git

### Проблема: 404 ошибки на страницах

**Решение:**
1. Убедитесь, что в `vercel.json` есть `rewrites` для SPA
2. Проверьте, что файл `vercel.json` существует и содержит правильную конфигурацию

### Проблема: Домен не работает

**Решение:**
1. Проверьте DNS записи в панели управления доменом
2. Убедитесь, что DNS записи правильные (используйте записи из Vercel)
3. Подождите распространения DNS (может занять до 48 часов)
4. Проверьте статус домена в Vercel Dashboard

---

## 📞 Полезные ссылки

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Документация Vercel:** https://vercel.com/docs
- **Документация Vite:** https://vitejs.dev/guide/static-deploy.html#vercel
- **Проверка DNS:** https://dnschecker.org/

---

## ✅ Чеклист

- [ ] Проект компилируется локально (`npm run build`)
- [ ] Все изменения закоммичены в Git
- [ ] Проект задеплоен в Vercel
- [ ] Домен добавлен в Vercel
- [ ] DNS записи настроены
- [ ] Сайт открывается по `https://ringoostroy.ru`
- [ ] Все страницы работают
- [ ] Изображения загружаются
- [ ] Формы работают
- [ ] SEO мета-теги правильные

---

**Готово!** Сайт должен быть доступен по адресу `https://ringoostroy.ru`

